首先要安装并配置Git：
先网页上注册一个git的账号
然后在自己电脑上安装git客户端（ubuntu默认已经安装好了，如果在windows下就需要另外安装）。

git init        ---创建本地仓库

ssh-keygen -t rsa -C "你的GitHub注册邮箱"-----------本地创建 SSH key，之后需要上传秘钥，这个用于将自己的文件上传到到服务器上你的仓库里面。

